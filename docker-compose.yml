services:
  # ================================
  # Local Development Service
  # ================================
  app-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"  # Vite dev server - Access at http://localhost:5173
      - "3000:3000"  # Express backend - Access at http://localhost:3000
    volumes:
      # Mount source code for hot-reload
      - ./src:/app/src
      - ./server:/app/server
      - ./index.html:/app/index.html
      - ./vite.config.js:/app/vite.config.js
      - ./tailwind.config.js:/app/tailwind.config.js
      - ./postcss.config.js:/app/postcss.config.js
      - ./package.json:/app/package.json
      # Prevent overwriting node_modules
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - PORT=3000
      - VITE_PORT=5173
      - VITE_API_URL=http://localhost:3000
    env_file:
      - .env.local
    restart: unless-stopped

# ================================
# Production Configuration (commented out for local dev)
# To use production mode, uncomment the sections below
# ================================
#
# services:
#   traefik:
#     image: "traefik"
#     restart: always
#     command:
#       - "--api=true"
#       - "--api.insecure=true"
#       - "--providers.docker=true"
#       - "--providers.docker.exposedbydefault=false"
#       - "--entrypoints.web.address=:80"
#       - "--entrypoints.web.http.redirections.entryPoint.to=websecure"
#       - "--entrypoints.web.http.redirections.entrypoint.scheme=https"
#       - "--entrypoints.websecure.address=:443"
#       - "--certificatesresolvers.mytlschallenge.acme.tlschallenge=true"
#       - "--certificatesresolvers.mytlschallenge.acme.email=${SSL_EMAIL}"
#       - "--certificatesresolvers.mytlschallenge.acme.storage=/letsencrypt/acme.json"
#     ports:
#       - "80:80"
#       - "443:443"
#     volumes:
#       - traefik_data:/letsencrypt
#       - /var/run/docker.sock:/var/run/docker.sock:ro
#
#   app:
#     build:
#       context: .
#       dockerfile: Dockerfile
#     restart: always
#     labels:
#       - traefik.enable=true
#       - traefik.http.routers.app.rule=Host(`${APP_DOMAIN}`)
#       - traefik.http.routers.app.tls=true
#       - traefik.http.routers.app.entrypoints=web,websecure
#       - traefik.http.routers.app.tls.certresolver=mytlschallenge
#       - traefik.http.middlewares.app-headers.headers.SSLRedirect=true
#       - traefik.http.middlewares.app-headers.headers.STSSeconds=315360000
#       - traefik.http.middlewares.app-headers.headers.browserXSSFilter=true
#       - traefik.http.middlewares.app-headers.headers.contentTypeNosniff=true
#       - traefik.http.middlewares.app-headers.headers.forceSTSHeader=true
#       - traefik.http.middlewares.app-headers.headers.SSLHost=${APP_DOMAIN}
#       - traefik.http.middlewares.app-headers.headers.STSIncludeSubdomains=true
#       - traefik.http.middlewares.app-headers.headers.STSPreload=true
#       - traefik.http.routers.app.middlewares=app-headers@docker
#       - traefik.http.services.app.loadbalancer.server.port=3000
#       - traefik.http.services.app.loadbalancer.healthcheck.path=/api/health
#       - traefik.http.services.app.loadbalancer.healthcheck.interval=30s
#     environment:
#       - NODE_ENV=production
#       - PORT=3000
#     env_file:
#       - .env
#     depends_on:
#       - traefik
#
#   n8n:
#     image: docker.n8n.io/n8nio/n8n
#     restart: always
#     ports:
#       - "127.0.0.1:5678:5678"
#     labels:
#       - traefik.enable=true
#       - traefik.http.routers.n8n.rule=Host(`${SUBDOMAIN}.${DOMAIN_NAME}`)
#       - traefik.http.routers.n8n.tls=true
#       - traefik.http.routers.n8n.entrypoints=web,websecure
#       - traefik.http.routers.n8n.tls.certresolver=mytlschallenge
#       - traefik.http.middlewares.n8n.headers.SSLRedirect=true
#       - traefik.http.middlewares.n8n.headers.STSSeconds=315360000
#       - traefik.http.middlewares.n8n.headers.browserXSSFilter=true
#       - traefik.http.middlewares.n8n.headers.contentTypeNosniff=true
#       - traefik.http.middlewares.n8n.headers.forceSTSHeader=true
#       - traefik.http.middlewares.n8n.headers.SSLHost=${DOMAIN_NAME}
#       - traefik.http.middlewares.n8n.headers.STSIncludeSubdomains=true
#       - traefik.http.middlewares.n8n.headers.STSPreload=true
#       - traefik.http.routers.n8n.middlewares=n8n@docker
#     environment:
#       - N8N_HOST=${SUBDOMAIN}.${DOMAIN_NAME}
#       - N8N_PORT=5678
#       - N8N_PROTOCOL=https
#       - NODE_ENV=production
#       - WEBHOOK_URL=https://${SUBDOMAIN}.${DOMAIN_NAME}/
#       - GENERIC_TIMEZONE=${GENERIC_TIMEZONE}
#     volumes:
#       - n8n_data:/home/node/.n8n
#       - /local-files:/files
#
# volumes:
#   traefik_data:
#     external: true
#   n8n_data:
#     external: true